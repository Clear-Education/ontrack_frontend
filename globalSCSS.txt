// Imports
@import "./mixins.scss";
// Css Variables
:root {
  --main-color: #7a00f9;
  --error: rgb(182, 60, 47);
}
// Sass variables
$font: -apple-system, BlinkMacSystemFont, Segoe UI, Roboto, Oxygen, Ubuntu,
  Cantarell, Fira Sans, Droid Sans, Helvetica Neue, sans-serif;
// Styles
html,
body {
  padding: 0;
  margin: 0;
  font-family: $font;
}
// General Clases
.center {
  text-align: center;
}
.left {
  text-align: left;
}
.right {
  text-align: right;
}
.margin-center {
  margin-left: auto;
  margin-right: auto;
}
/* General Modal Styles */
.close_modal {
  width: 40px;
  position: absolute;
  top: 10px;
  right: 10px;
  cursor: pointer;
  z-index: 10;
  background-color: #00000028;
  border-radius: 100%;
  padding: 10px;
}
.MuiDialogTitle-root {
  .MuiTypography-root {
    font-weight: 700;
    font-family: $font;
  }
}
/* General form styles */
.inputs-container {
  margin-top: 20px;
  .MuiFormControl-root {
    width: 100%;
    .MuiFormLabel-root {
      text-transform: capitalize;
      font-family: $font !important;
    }
    .MuiInputBase-root,
    .MuiFormHelperText-root {
      font-family: $font !important;
    }
    .MuiFormHelperText-contained {
      margin: 5px 0 0 0;
    }
    // For outlined inputs
    .MuiOutlinedInput-root.Mui-focused.Mui-focused fieldset {
      border-color: var(--main-color);
    }
    .MuiFormLabel-root.Mui-focused {
      color: var(--main-color) !important;
    }
    // For standard inputs
    .MuiInput-underline:after {
      border-bottom: 2px solid var(--main-color) !important;
    }
    .MuiFormLabel-root.Mui-focused {
      color: var(--main-color) !important;
    }
    .MuiInputBase-root.Mui-focused:after {
      border-bottom: 2px solid var(--main-color);
    }
    // For error inputs
    .Mui-error:after {
      border: 2px solid var(--error) !important;
    }
    .MuiOutlinedInput-root.Mui-error.Mui-error fieldset {
      border-color: var(--error);
    }
    .MuiFormLabel-root.Mui-error,
    .MuiFormHelperText-root.Mui-error,
    .MuiFormLabel-asterisk.Mui-error {
      color: var(--error) !important;
    }
  }
}
/* Pickers */
.MuiPickersModal-dialogRoot {
  .MuiTypography-root {
    font-family: $font;
  }
  // Date picker
  .MuiPickersToolbar-toolbar,
  .MuiPickersDay-daySelected,
  .MuiPickersDay-daySelected:hover {
    background-color: var(--main-color) !important;
  }
  .MuiTypography-colorPrimary {
    color: var(--main-color) !important;
  }
  .MuiDialogActions-root {
    button {
      font-family: $font;
      background-color: var(--main-color) !important;
      border-radius: 20px;
      text-transform: capitalize;
      color: #ffffff !important;
    }
  }
  // Time picker
  .MuiPickersClock-pin,
  .MuiPickersClockPointer-pointer,
  .MuiPickersClockPointer-noPoint {
    background-color: var(--main-color) !important;
    .MuiPickersClockPointer-thumb {
      border: 14px solid var(--main-color);
    }
  }
  // DateTime picker
  .MuiPickerDTTabs-tabs {
    background-color: var(--main-color) !important;
  }
  .MuiTabs-indicator {
    background-color: #ffffff;
  }
  .MuiTab-root {
    font-family: $font;
  }
}
/* Circular progress */
.MuiCircularProgress-colorPrimary {
  color: #ffffff !important;
}
.MuiCircularProgress-colorSecondary {
  color: #000000 !important;
}

@include media(md) {
  /* Modales fullscreen comunes */
  .MuiDialog-paperFullScreen {
    border-radius: 0px !important;
  }
  /* Modales responsive comunes */
  .responsive_modal {
    top: auto !important;
  }
  .responsive_modal {
    .MuiDialog-paperFullScreen {
      max-height: 86vh !important;
    }
    .MuiBackdrop-root {
      background-color: rgba(0, 0, 0, 0.3) !important;
    }
    .MuiDialog-paperFullScreen {
      border-radius: 20px 20px 0 0 !important;
    }
  }
}
